# CLAUDE.md

## CDK 作業ルール

- CDK 変更に着手する前に、必ずユーザーへ「今、deploy/destroy は実行中ですか？」と確認する
- ユーザーが実行中と回答した場合、CDK 変更は停止して待機する
- 実行状態が不明な場合は、状態が明確になるまで CDK 変更を行わない
- CDK で「その場しのぎ」の回避コードは入れず、恒久設計で解決する
- stateful リソース（DB/Bucket など）の構造変更時は、`cdk diff` で置換有無を確認する
