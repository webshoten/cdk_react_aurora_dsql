# Resume - 2026-04-28

## 本日の到達点

- `11-3.auth-01` の仕様を整理・更新
- `2.infra-resources` / `1.architecture` / `2-3.api-stack` を `AuthStack` 分離方針に合わせて更新
- `overview.md` 記述ルールを更新（確定仕様のみ記載、タスクは `plan/` に分離）

## 確定方針（要約）

- 認証基盤は `AuthStack` として分離
- ログインは `/login`、デバッグ機能は `main` と同列の `/debug`
- `/debug` は認証済みユーザー全員アクセス可
- ユーザー作成は `script` と `/debug`（mutation 直実行）の2経路
- ユーザー一覧は全件表示（ページング/検索なし）

## 次回の進め方

1. 関連 `overview.md` の読み合わせ（ユーザーとCodexの認識合わせ）
2. 認識確定後に実装へ着手
