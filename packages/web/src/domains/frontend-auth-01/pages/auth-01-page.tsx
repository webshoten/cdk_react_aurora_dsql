import { PlaceholderPage } from "@/shared/ui/placeholder-page.tsx";

/*
 * # 4-3.auth-01 ページ（プレースホルダ）
 *
 * ## 目的
 * 認証フロー検証用ページの枠だけ確保する未実装ページ。
 *
 * ## NOTE
 * - 実装着手時に PlaceholderPage を実コンテンツへ差し替える。
 */
export function Auth01Page() {
  return <PlaceholderPage title="4-3.auth-01" description="4-3 auth flow validation" />;
}
